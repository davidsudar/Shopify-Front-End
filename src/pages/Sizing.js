import SizeGuide from "../components/SizeGuide";

const Sizing = () => (
  <div className="contact">
    <h1>LONG & SHORT SLEEVE SIZE GUIDE</h1>
    <p>All sizes are designed to fit to the following body measurements:</p>
    <p>
      Some brands may vary from these measurements but you can still use them as
      a guide.
    </p>

    <SizeGuide />
  </div>
);

export default Sizing;
